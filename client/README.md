# Hola

#### Build Setup

``` bash
# install dependencies
npm install

# serve with hot reload at localhost:9080
npm run dev

# 构建软件包
# Windows 32位
npm run build:win32

# Windows 64位
npm run build:win64

# run unit tests
npm test

# lint all JS/Vue component files in `src/`
npm run lint
```

